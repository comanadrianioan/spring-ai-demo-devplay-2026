server:
  port: 8080
spring:
  application:
    name: devplay-2026
  ai:
    mcp:
      server:
        # SSE (HTTP) transport is required for Docker / Render.com deployments.
        # The default STDIO transport only works for local process communication.
        transport: SSE
